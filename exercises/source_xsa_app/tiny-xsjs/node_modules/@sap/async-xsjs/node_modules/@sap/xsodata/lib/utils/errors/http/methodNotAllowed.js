'use strict';
var util = require('util');
var HttpError = require('./../httpError');

module.exports = MethodNotAllowed;

/**
 * Error indicating an XSOData internal problem, e.g. used in try/catch statements to catch runtime errors
 * @param message
 * @param cause
 * @param context
 * @constructor
 */
function MethodNotAllowed(message, context, cause) {
    HttpError.call(this, message, context,cause, 405);
    this.context = context;
}

util.inherits(MethodNotAllowed, HttpError);

