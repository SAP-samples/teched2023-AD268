'use strict';
var util = require('util');
var HttpError = require('./../httpError');

module.exports = UnsupportedMediatype;

function UnsupportedMediatype(message, context, cause) {
    HttpError.call(this, message, context,cause, 415);
    this.context = context;
}

util.inherits(UnsupportedMediatype, HttpError);

