'use strict';

var MANDATORY_OPTIONS = ['url', 'clientid', 'sm_url'];
var MANDATORY_OR_OPTIONS = ['clientsecret', 'certificate'];

var MIN_TOKEN_VALIDITY_SECONDS = 30;

var HTTP_STATUS = {
  OK: 200,
  ASYNC_OPERATION_STARTED: 202,
  NOT_FOUND: 404,
  CONFLICT: 409
};

var RESOURCE_STATE = {
  SUCCEEDED: 'succeeded',
  IN_PROGRESS: 'in progress',
  FAILED: 'failed'
};

var ERRORS = {
  CONFLICT: 'Conflict'
};

module.exports = {
  MANDATORY_OPTIONS: MANDATORY_OPTIONS,
  MANDATORY_OR_OPTIONS: MANDATORY_OR_OPTIONS,
  MIN_TOKEN_VALIDITY_SECONDS: MIN_TOKEN_VALIDITY_SECONDS,
  STATUS: { HTTP: HTTP_STATUS },
  RESOURCE: { STATE: RESOURCE_STATE },
  ERRORS: ERRORS,
  LIBRARY: { LABEL_KEY: 'managing_client_lib', LABEL_VALUE: 'instance-manager-client-lib' },
  TIME_PROPERTY: 'created_at',
  DEFAULT_SERVICE: 'hana',
  DEFAULT_PLAN: 'hdi-shared',
  DEFAULT_ALLOW_BINDING: true
};
